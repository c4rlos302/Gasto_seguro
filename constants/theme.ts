import { Colors } from "./colors";

export const lightColors = {
  fondo: Colors.fondoLight,
  principal: Colors.principalLight,
  secundario: Colors.secundarioLight,
  chip: Colors.chipLight,
  overlay: Colors.overlayLight,
  link: Colors.linkLight,

  text: Colors.textPrimaryLight,
  textSecondary: Colors.textSecondaryLight,
  border: Colors.borderLight,
};

export const darkColors = {
  fondo: Colors.fondoDark,
  principal: Colors.principalDark,
  secundario: Colors.secundarioDark,
  chip: Colors.chipDark,
  overlay: Colors.overlayDark,
  link: Colors.linkDark,

  text: Colors.textPrimaryDark,
  textSecondary: Colors.textSecondaryDark,
  border: Colors.borderDark,
};